# three-starter

Minimal **Vite + vanilla JS** Three.js starter that renders a rotating cube.

## Run

```bash
cd three-starter
npm install
npm run dev
```

## Build / preview

```bash
cd three-starter
npm run build
npm run preview
```

